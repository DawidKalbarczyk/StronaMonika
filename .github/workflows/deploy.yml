name: Deploy react app to github pages
on:
  push:
    branches:
      - master

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: config/node/frontend
    steps:
      - name: Sprawdzenie
        uses: actions/checkout@v4

      - name: Przygotuj Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: config/node/rontend/package-lock.json

      - name: Instaluj zaleznosci
        run: npm ci

      - name: Zkompiluj
        run: npm run build

      - name: Wrzuc artefakty
        uess: actions/upload-pages-artifact@v3
        with:
          path: config/node/frontend/build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Wrzucenie na github pages
        id: deployment
        uses: actions/deploy-pages@v4
